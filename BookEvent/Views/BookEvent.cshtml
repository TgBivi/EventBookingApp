@using Microsoft.AspNetCore.Mvc.TagHelpers;
@using Core.ViewModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<BookEventViewModel>
@{
	// Layout = "master.cshtml";		
	ViewData["Title"] = "Booking";
}


@await Html.PartialAsync("_booking", Model);
<div class="container">

	@* @if (TempData["Success"] != null)
	{
		<div class="alert alert-success">
			@TempData["Success"]
		</div>
	}

	@if (TempData["Error"] != null)
	{
		<div class="alert alert-danger">
			@TempData["Error"]
		</div>
	}
	@if(Model != null)
	{
		@using (Html.BeginForm("BookEvent","EventController", FormMethod.Post))
		{
			<div class="row section">
				<div class="contact-form col-8 mx-auto shadow-lg py-3">
					<div class="form-group">
						<label for="firstName">@Model.FirstNameL</label>
						<input type="text" class="form-control" id="firstName" name="FirstName" required>
					</div>
					<div class="form-group">
						<label for="surname">@Model.SurNameLabel</label>
						<input type="text" class="form-control" id="surname" name="Surname" required>
					</div>
					<div class="form-group">
						<label for="email">@Model.EmailLabel</label>
						<input type="email" class="form-control" id="email" name="Email" required>
					</div>
					<div class="form-group">
						<label for="phoneNumber">@Model.PhoneLabel</label>
						<input type="tel" class="form-control" id="phoneNumber" name="PhoneNumber" >
					</div>
					<input type="text" class="form-control" id="eventId" name="EventId" hidden>
					<button type="submit" class="btn btn-primary">@Model.ButtonText</button>
				</div>
			</div>
		}
	}
	else
	{
		<div class="card col-4 m-auto shadow-lg" style="min-height: 150px;">
		<div class="card-body">

			<h5 class="card-title">This event is fully booked, try another one</h5>
		</div>
		<div class="pb-2 text-center">
			<a href="/event" class="btn btn-primary">Back</a>
		</div>
	</div>
	} *@

</div>